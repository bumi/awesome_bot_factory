require "bundler/setup"
Bundler.require(:default, :production)
require File.join(File.dirname(__FILE__), "<%= skill_file_name %>.rb")

use Rack::CommonLogger
map '/<%= skill_file_name %>' do
  run <%= skill_class_name %>
end